# docs/archive — histórico

Documentación **congelada, no vigente**. Se conserva solo como registro histórico; no es fuente de verdad.

- `phase-1/`, `phase-2/` — documentación de marzo 2026, **previa a la migración a las 4 APIs por dominio**
  (identity/academic/learning/platform). Usa nombres muertos (`api-iam-platform`, `admin-new`,
  `mobile-new`) y enlaza a la carpeta `Common/` que ya no existe.

Para el estado actual del repo, ver `../README.md`, el `AGENTS.md` raíz y el `docs/` de cada módulo.
